{
  "name": "foundry",
  "scripts": {
    "install": "forge install && forge update",
    "test": "forge test",
    "build": "forge build",
    "deploy": "forge script script/HelloWorld.s.sol --broadcast"
  },
  "devDependencies": {
    "solc": "0.8.28"
  }
}
