{
  "name": "foundry",
  "scripts": {
    "install": "forge install && forge update",
    "test": "forge test",
    "build": "forge build",
    "broadcast": "forge script script/HelloWorld.s.sol -f sepolia --verify --broadcast"
  },
  "devDependencies": {
    "solc": "0.8.28"
  }
}
